.shipping_methods-container
  %table.fancy.sortable.shipping_methods
    %thead
      %tr
        %th.name Name
        %th.type Type
        %th.price Price
        %th.actions Actions
    %tbody
      - if shipping_methods.any?
        = render :partial => "breeze/admin/commerce/shipping_methods/shipping_method", :collection => shipping_methods, locals: {count: shipping_methods.count}
      - else
        %tr.empty
          %td.empty{:colspan => "6"} There are no shipping methods to show here.
    %tfoot
      %tr