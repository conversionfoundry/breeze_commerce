var js_products_array = <%= raw @products.to_json %>;
$.each(js_products_array, function(index, product) { 
  $('#product_' + product._id + ' .available').html(product.available ? 'Available' : 'Not Available').effect("highlight", { color: "#ffffc0" }, 3000); 
});