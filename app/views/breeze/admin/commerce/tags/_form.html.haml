= breeze_form_for @tag, :as => :tag, :url => url, :remote => true do |form|
  = render "breeze/admin/shared/error_messages", :target => @tag, :object_name => :tag
  = form.fieldset do
    = form.text_field :name