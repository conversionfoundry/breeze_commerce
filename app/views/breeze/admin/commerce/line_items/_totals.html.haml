%tr.subtotal
  %td{:colspan => "4"}
  %td Subtotal:
  %td.total
    = number_to_currency @order.item_total
    %span.currency NZD
%tr.shipping
  %td{:colspan => "4"}
  %td
    Shipping:
  %td
    - Breeze::Commerce::Store.first.shipping_methods.unarchived.each do |shipping_method|
      %label.radio
        = form.radio_button :shipping_method_id, shipping_method.id
        %h4
          = shipping_method.name
        %p.description
          = shipping_method.description
  %td.total
    = number_to_currency @order.shipping_total
    %span.currency NZD
%tr.total
  %td{:colspan => "4"}
  %td Total cost:
  %td.total
    = number_to_currency @order.total
    %span.currency NZD