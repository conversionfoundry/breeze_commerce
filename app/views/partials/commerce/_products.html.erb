<% if products.any? %>
  <%= content_tag :ul, :class => "products hfeed" do %>
    <%= render :partial => "/partials/commerce/product", :collection => products %>
  <% end %>
<% else %>
  <p>There are no products to display.</p>
<% end %>
